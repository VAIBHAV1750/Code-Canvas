# Code-Canvas
Code Canvas is a visual drag-and-drop website builder where users can create web pages without manually writing HTML or CSS. Every element added, moved, edited, or styled on the canvas automatically generates real-time HTML and CSS code in the background, and the copied code produces the exact same output when run in VS Code.
